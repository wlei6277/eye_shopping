<%= image_tag current_user.picture %>
<h2><%= current_user.picture %></h2>
<h3>Follower# <%= @followers_count %></h3>
<!-- NEED TO PUT IN THE DONATION COUNT!!!!!!!!!!!! -->

<%= link_to 'Add Product', new_product_path %>

<!-- Page Content -->
<div class="container">
            <% @products.each do |product| %>
            <%= image_tag product.picture if product.picture.attached? %></td>
            <% end %>
        
  <a class="nav-link"  ></a>
    <td><%= link_to 'Destroy', @product, method: :delete, data: { confirm: 'Are you sure?' } %></td>

  <hr>

  <!-- CTA -->
  <a>
  
  <%= link_to 'Home', root_path %>
  </a>
 
</div>

<!-- Footer -->
    <hr> 
    <footer>
        
<!-- /.container -->