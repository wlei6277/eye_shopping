<% if current_user.user_favourites(current_user: current_user.id, product: id) %>
    <%= link_to 'Unlike', favourite_path(@favourite) , method: :delete %>
<% else %>
    <%= link_to 'Like', favourites_path(user_id: current_user.id, product_id: id), method: :post %>
<% end %>